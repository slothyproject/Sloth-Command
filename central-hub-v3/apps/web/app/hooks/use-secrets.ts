/**
 * Secrets Hooks
 * Re-exported from use-automation for backward compatibility
 */

export { 
  useSecrets,
  useSecret,
  useCreateSecret,
  useUpdateSecret,
  useDeleteSecret,
  useRotateSecret,
  useBulkImportSecrets,
  type Secret 
} from './use-automation';
